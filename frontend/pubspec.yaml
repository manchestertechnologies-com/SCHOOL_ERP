name: manchester_school_bridge
description: Modern school management and communication platform frontend client.
version: 1.0.0+1

publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.3.6
  go_router: ^10.0.0
  dio: ^5.2.1
  flutter_secure_storage: ^8.0.0
  uuid: ^3.0.7
  intl: ^0.18.1
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
